{
  "name": "sentry-electron-sample",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean:linux": "rimraf ./dist/linux*",
    "build:linux-dev": "npm run clean:linux && electron-builder --linux"
  },
  "author": "moritamorie",
  "license": "ISC",
  "dependencies": {
    "@sentry/electron": "1.3.0"
  },
  "devDependencies": {
    "electron-builder": "^22.7.0",
    "electron": "^9.3.1"
  }
}
